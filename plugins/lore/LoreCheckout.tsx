import React from "react"
import CartButton from "./CartButton"

export const LoreCheckout = () => {
  
  return (
    <div style={{position: 'fixed', bottom: 10, right: 10, zIndex: 100}}>
      <CartButton />
    </div>
  )
}
